# scaffold

Guided, wizard-style creation of a new `assess-*` lens, standalone workflow, or command:
generate from the existing pattern, wire the manifest, and regenerate the shims. Authoring/
meta; edits framework files only. Run `/scaffold`, or from any agent: "read and execute
`.agents/workflows/scaffold/scaffold.md`".
