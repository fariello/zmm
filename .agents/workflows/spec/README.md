# spec

Front of the funnel: turn a fuzzy request into a reviewable specification (goals,
non-goals, users, requirements, test criteria) before planning or building. Run
`/spec [request]`, or from any agent: "read and execute `.agents/workflows/spec/spec.md`".
