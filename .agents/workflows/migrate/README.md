# migrate

Assess-and-plan a high-risk migration (framework, database, dependency-major, or layout):
inventory the blast radius, sequence the change safely, and emit an IPD. Run `/migrate`, or
from any agent: "read and execute `.agents/workflows/migrate/migrate.md`".
