# Agent Workflows

This directory contains the neutral, tool-agnostic workflow instructions (manifest and definitions) for `agent-workflows`.

To install or sync these workflows in a repository, run:
  aw install <dir>
