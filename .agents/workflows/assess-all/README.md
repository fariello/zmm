# assess-all

Cross-concern rollup: run the assess family (all concerns, a group, or a subset - it
confirms scope and cost first) and consolidate the findings into one prioritized,
cross-concern IPD. Run `/assess-all`, or from any agent: "read and execute
`.agents/workflows/assess-all/assess-all.md`".
